// Checking vowel or consonent

let ch = "N";
if(ch==="a"||ch==="e"||ch==="i"||ch==="o"||ch==="u"||ch==="A"||ch==="E"||ch==="I"||ch==="O"||ch==="U") {
  console.log(`${ch} is a vowel`);
}else {
  console.log(`${ch} is a consonent`);
}

// Output = N is a consonent